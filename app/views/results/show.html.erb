<section class="hero is-success">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        釣果詳細情報
      </h1>
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-7">
        <div class="card">
          <div class="card-image">
            <figure class="image is-4by3">
              <%= attachment_image_tag @result, :image %>
            </figure>
          </div>
          <div class="card-content">
            <div class="media">
              <div class="media-content">
                <p class="title is-4"><%= @result.title %></p>
              </div>
            </div>
            <div class="content">
              <table class="table is-narrow">
                <tr>
                  <th>詳細情報、感想など</th>
                </tr>
                <tr>
                  <td><%= simple_format @result.body %></td>
                </tr>
              </table>
              <% if @result.user.id == current_user.id %>
                <%= link_to "編集画面へ", edit_result_path(@result), class: "button is-success" %>
              <% end %>
            </div>
          </div>
        </div>
      </div>

      <div class="column is-4">
        <article class="panel is-link">
          <p class="panel-heading">
            By <%= @result.user.profile %>
          </p>
          <div class="panel-block">
            <p class="control">
              <%= @result.user.profile %>
            </p>
          </div>
          <%= link_to user_path(@result.user), class: "panel-block" do %>
            <span class="panel-icon">
              <i class="fas fa-user" aria-hidden="true"></i>
            </span>
            <%=@result.user.username %> さんについて
          <% end %>
        </article>
      </div>
    </div>
  </div>
</section>